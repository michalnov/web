var lastPosition = 0;

function draw()
{
    var canvas = document.getElementById("myCanvas");
    var board = canvas.getContext("2d");
    var temp = parseInt(document.getElementById("in").value);
    
}